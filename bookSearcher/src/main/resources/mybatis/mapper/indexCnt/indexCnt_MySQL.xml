<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.core.module.indexCnt.repository.IndexCntRepository">
	<select id="findIndexCnt" parameterType="indexCnt" resultType="indexCnt">
		SELECT 
			A.INDEX_SEQ 
			, A.INDEX_NAME 
			, A.INDEX_CNT
		FROM 
			INDEX_CNT A
		WHERE 
			1=1
			AND A.INDEX_NAME = #{indexName}
	</select>

	<insert id="createIndexCnt" parameterType="indexCnt">
		INSERT INTO INDEX_CNT
		(
			INDEX_SEQ
			, INDEX_NAME
			, INDEX_CNT)
		VALUES(
			#{indexSeq}
			, #{indexName}
			, #{indexCnt}
			)
	</insert>

	<delete id="deleteIndexCnt" parameterType="indexCnt">
		DELETE FROM 
			INDEX_CNT
		WHERE 
			1=1
			INDEX_SEQ=#{indexSeq}
	</delete>
</mapper>